use strict
$(document).ready(function() {
	('.send_btn').on('click', function( event ) {
		event.preventDefault();
		const form = $('#form');
		if ( form.find( 'input[name="mf_name"]' ).val() == '' ) {
			alert('Ты тупой');
	}
		if ( form.find( 'input[name="mf_name"]' )) {
			
	}
		if ( form.find( 'input[name="mf_name"]' )) {
			
	}
		if ( form.find( 'input[name="mf_name"]' )) {
			
	}
		if ( form.find( 'input[name="mf_name"]' )) {
			
	}
		if ( form.find( 'input[name="mf_name"]' )) {
			
	}
});